for example you can download
http://jslinq.codeplex.com

example:

    var app={};
    app.jsinc            = require('deps/jsinc').jsinc;
    app.linq             = app.jsinc(__dirname+'/deps/jslinq/scripts/JSLINQ.js').window.JSLINQ;
    console.log(require('util').inspect(app.linq));

    var tiles            = app.linq(map_layers.tiles).Where(function(item){ return !(
        item.x<-255
      ||item.y<-255
      ||item.x>map_width
      ||item.y>map_height) }).items;
    console.log("Invite : " , require('util').inspect(tiles));
